// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCSIUDZIhb3OwDj4-bePFmSQ02NNPzcGjI",
    authDomain: "fir-course-defe6.firebaseapp.com",
    databaseURL: "https://fir-course-defe6.firebaseio.com",
    projectId: "fir-course-defe6",
    storageBucket: "fir-course-defe6.appspot.com",
    messagingSenderId: "895164517381",
    appId: "1:895164517381:web:72d64182d942774747606c"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
